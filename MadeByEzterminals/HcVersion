-- By ezterminals on / New Tiktok Give credits or raided.
-- Beta, You may think this is bad, But Test it.
-- Version:TerminalWare V1
-- Change Sets if you like.
-- Sets for ping 100 - 0
-- 3%534:8;@“#
-- Any Copy Of This Script Will be taken down. And ACCOUNT suspended.
-- .gg/8yYZc4CHHp
-- If you see the owner change at any point it will be me
-- This is the original script and it always will be.
-- Coped, Ezterminals on TikTok follow me.
loadstring(game:HttpGet("https://raw.githubusercontent.com/Pixeluted/adoniscries/main/Source.lua",true))()
getgenv().targetstrafe = {
    speed = 15,
    height = 6,
    size = 6,
}

local aimParts = {"HumanoidRootPart", "Head", "RightLeg", "LeftLeg", "RightArm", "LeftArm", "UpperTorso"}
local SelectedPart = "Head"
local Prediction = true
local PredictionValue = 0.117392
local NoGroundShots = true
local AutoPred = true
Vector3.new(0, 0.1334, 0)

local function groundShots()
Prediction = math.clamp(Velocity.Y * PredictionValue, -0.855, math.huge)
end
function FindNearestEnemy()
    local LocalPlayer = game.Players.LocalPlayer
    local ClosestDistance, ClosestPlayer, ClosestPart = math.huge, nil, nil

    for _, Player in ipairs(game.Players:GetPlayers()) do
        if Player ~= LocalPlayer then
            local Character = Player.Character
            if Character and Character:FindFirstChild("Humanoid") and Character.Humanoid.Health > 0 then
                local Part = Character:FindFirstChild(SelectedPart)
                if Part then
                    local Position, IsVisibleOnViewPort = game:GetService("Workspace").CurrentCamera:WorldToViewportPoint(Part.Position)
                    if IsVisibleOnViewPort then
                        local Distance = (LocalPlayer.Character.Head.Position - Part.Position).Magnitude
                        if Distance < ClosestDistance then
                            ClosestPlayer = Player
                            ClosestPart = Part
                            ClosestDistance = Distance
                        end
                    end
                end
            end
        end
    end

    return ClosestPlayer, ClosestPart
end

local CC = game:GetService("Workspace").CurrentCamera
local Plr, AimPart
local enabled = true
local targetstrafe_enabled = true

local RunService = game:GetService("RunService")

local gui = Instance.new("ScreenGui")
gui.Name = "patrickGui"
gui.Parent = game:GetService("CoreGui")

local TextButton = Instance.new("TextButton")
TextButton.Text = "Toggle Bate Aimlock"
TextButton.TextSize = 22
TextButton.TextColor3 = Color3.new(1, 1, 1)
TextButton.BackgroundColor3 = Color3.new(0, 0, 0)
TextButton.BorderColor3 = Color3.new(1, 1, 1)
TextButton.BorderSizePixel = 4
TextButton.Font = Enum.Font.Code
TextButton.Size = UDim2.new(0.2, 0, 0.1, 0)
TextButton.Position = UDim2.new(0, 0, 0.5, 0)
TextButton.Parent = gui
TextButton.Draggable = true

local StrafeButton = Instance.new("TextButton")
StrafeButton.Text = "Disable targetstrafe"
StrafeButton.TextSize = 22
StrafeButton.TextColor3 = Color3.new(1, 1, 1)
StrafeButton.BackgroundColor3 = Color3.new(0, 0, 0)
StrafeButton.BorderColor3 = Color3.new(1, 1, 1)
StrafeButton.BorderSizePixel = 4
StrafeButton.Font = Enum.Font.Code
StrafeButton.Size = UDim2.new(0.2, 0, 0.1, 0)
StrafeButton.Position = UDim2.new(0, 0, 0.3, 0)
StrafeButton.Parent = gui
StrafeButton.Draggable = true

local state = false
local greenMarker = nil

function makemarker(Parent, Adornee, Color, Size, Size2)
    local e = Instance.new("BillboardGui", Parent)
    e.Name = "GreenMarker"
    e.Adornee = Adornee
    e.Size = UDim2.new(Size, Size2, Size, Size2)
    e.AlwaysOnTop = true
    local a = Instance.new("Frame", e)
    a.Size = UDim2.new(1, 0, 1, 0)
    a.BackgroundColor3 = Color
    local g = Instance.new("UICorner", a)
    g.CornerRadius = UDim.new(1, 1)
    return e
end

function updateMarkerPosition()
    if greenMarker and AimPart then
        greenMarker.Adornee = AimPart
    end
end

function toggleBate()
    state = not state
    if state then
        TextButton.Text = "Bate ON"
        enabled = true
        Plr, AimPart = FindNearestEnemy()
        if AimPart then
            if greenMarker then
                greenMarker:Destroy()
            end
            greenMarker = makemarker(game.Workspace, AimPart, Color3.new(0, 1, 0), 0.3, 3)
            updateMarkerPosition()
        end
    else
        TextButton.Text = "Toggle Bate Aimlock"
        enabled = false
        if greenMarker then
            greenMarker:Destroy()
            greenMarker = nil
        end
    end
end

TextButton.MouseButton1Click:Connect(toggleBate)

StrafeButton.MouseButton1Click:Connect(function()
    targetstrafe_enabled = not targetstrafe_enabled
    if targetstrafe_enabled then
        StrafeButton.Text = "Disable targetstrafe"
    else
        StrafeButton.Text = "Enable targetstrafe"
    end
end)

local function isAboveGround(position)
    local ray = Ray.new(position, Vector3.new(0, 0.06, 0) * 5)
    local part, hitPosition = game.Workspace:FindPartOnRay(ray, game.Workspace, false, true)
    return not part
end

function getTargetPosition()
    if AimPart then
        local pos = AimPart.Position
        return { x = pos.X, y = pos.Y, z = pos.Z }
    end
    return nil
end

function setCharacterOrientation(x, y, z)
    local player = game.Players.LocalPlayer
    local character = player.Character
    if character then
        local hrp = character:FindFirstChild("HumanoidRootPart")
        if hrp then
            hrp.CFrame = CFrame.new(x, y, z)
        end
    end
end

function getCurrentTime()
    return os.clock()
end

function PreventGroundHits()
    local player = game.Players.LocalPlayer
    local character = player.Character
    if character then
        local hrp = character:FindFirstChild("HumanoidRootPart")
        if hrp then
            local currentPosition = hrp.Position
            
            local ray = Ray.new(currentPosition, Vector3.new(0, 0.06, 0) * 50)
            local part, hitPosition = game.Workspace:FindPartOnRay(ray, game.Workspace, false, true)
            
            if part and hitPosition.Y < currentPosition.Y then
                local newPosition = Vector3.new(currentPosition.X, hitPosition.Y + 5, currentPosition.Z)
                hrp.CFrame = CFrame.new(newPosition)
            end
        end
    end
end

function update()
    if enabled and state and greenMarker and AimPart and targetstrafe_enabled then
        local currentTime = getCurrentTime()
        local angle = currentTime * getgenv().targetstrafe.speed
        
        local targetPos = getTargetPosition()
        if targetPos then
            local offsetX = getgenv().targetstrafe.size * math.cos(angle)
            local offsetZ = getgenv().targetstrafe.size * math.sin(angle)
            local offsetY = getgenv().targetstrafe.height  -- Apply vertical offset
            
            local newPos = Vector3.new(targetPos.x + offsetX, targetPos.y + offsetY, targetPos.z + offsetZ)
            
            if NoGroundShots and not isAboveGround(newPos) then
                local ray = Ray.new(newPos, Vector3.new(0, 0.06, 0) * 100)
                local part, hitPosition = game.Workspace:FindPartOnRay(ray, game.Workspace, false, true)
                if part then
                    newPos = hitPosition + Vector3.new(0, 2, 0)
                end
            end

            PreventGroundHits()
            setCharacterOrientation(newPos.x, newPos.y, newPos.z)
        end
    end
end

RunService.RenderStepped:Connect(update)

RunService.Stepped:Connect(function()
    if enabled then
        PredictionValue = generatePredictionValue()
    end
end)

local function generatePredictionValue()
    return PredictionValue
end

local function updatePredictionValue()
    if AutoPred then
        local ping = tonumber(Stats.Network.ServerStatsItem["Data Ping"]:GetValueString():split(".")[1])
        if ping < 20 then
            PredictionValue = 0.11378905
        elseif ping < 30 then
            PredictionValue = 0.1222789
        elseif ping < 40 then
            PredictionValue = 0.12948805
        elseif ping < 50 then
            PredictionValue = 0.137988957
        elseif ping < 60 then
            PredictionValue = 0.1454058899
        elseif ping < 70 then
            PredictionValue = 0.1542885922
        elseif ping < 80 then
            PredictionValue = 0.158893
        elseif ping < 90 then
            PredictionValue = 0.16228392
        elseif ping < 100 then
            PredictionValue = 0.168799
        elseif ping < 110 then
            PredictionValue = 0.172298805
        elseif ping < 120 then
            PredictionValue = 0.17538895
        elseif ping < 130 then
            PredictionValue = 0.181183905
        elseif ping < 140 then
            PredictionValue = 0.1868334505
        elseif ping < 150 then
            PredictionValue = 0.1888428905
        elseif ping < 160 then
            PredictionValue = 0.189328482
        elseif ping < 170 then
            PredictionValue = 0.192283831
        elseif ping < 180 then
            PredictionValue = 0.198837284
        elseif ping < 190 then
            PredictionValue = 0.20348289
        elseif ping < 200 then
            PredictionValue = 0.209739292
        end
    end
end

local mt = getrawmetatable(game)
local old = mt.__index
setreadonly(mt, false)

local lastTarget = nil

mt.__index = newcclosure(function(self, key)
    if not checkcaller() and enabled and typeof(self) == "Instance" and self:IsA("Mouse") and key == "Hit" then
        if Plr and Plr.Character and Plr.Character:FindFirstChild(SelectedPart) and Plr.Character:FindFirstChild("Head") then
            local target = Plr.Character[SelectedPart]
            if target ~= lastTarget then
                lastTarget = target
            end

            local predictedPosition = target.Position + (Plr.Character.Head.Velocity * PredictionValue)
            return CFrame.new(predictedPosition)
        end
    end
    return old(self, key)
end)

setreadonly(mt, true)

local delayTime = 0.182489-- Reduc response
local lastShotTime = 0.182489-- Reduced 0.186394
local autoShootEnabled = false

function autoShoot()
    if autoShootEnabled then
        local tool = game.Players.LocalPlayer.Character:FindFirstChildOfClass("Tool")
        if tool and Plr then
            local humanoid = Plr.Character:FindFirstChildOfClass("Humanoid")
            if humanoid then
                local state = humanoid:GetState()
                if state == Enum.HumanoidStateType.Freefall then -- Freefall might be better for detecting air shots
                    local currentTime = tick()
                    if currentTime - lastShotTime >= delayTime then
                        tool:Activate()
                        lastShotTime = currentTime
                    end
                end
            end
        end
    end
end

-- GUI setup
local gui = Instance.new("ScreenGui")
gui.Name = "autoAirGui"
gui.Parent = game:GetService("CoreGui")

local toggleButton = Instance.new("TextButton")
toggleButton.Text = "Auto Air Off"
toggleButton.TextSize = 22
toggleButton.TextColor3 = Color3.new(1, 1, 1)
toggleButton.BackgroundColor3 = Color3.new(0, 0, 0)
toggleButton.BorderColor3 = Color3.new(1, 1, 1)
toggleButton.BorderSizePixel = 4
toggleButton.Font = Enum.Font.Code
toggleButton.Size = UDim2.new(0.08, 0, 0.1, 0)
toggleButton.Position = UDim2.new(0, 0, 0.5, 0)
toggleButton.Parent = gui
toggleButton.Draggable = true

toggleButton.MouseButton1Click:Connect(function()
    autoShootEnabled = not autoShootEnabled
    if autoShootEnabled then
        toggleButton.Text = "Auto Air On"
    else
        toggleButton.Text = "Auto Air Off"
    end
end)

-- Call autoShoot regularly
RunService.Heartbeat:Connect(autoShoot)

local lookAtEnabled = false
local gui = Instance.new("ScreenGui", game.Players.LocalPlayer:WaitForChild("PlayerGui"))

local lookAtButton = Instance.new("TextButton")
lookAtButton.Text = "Enable LookAt"
lookAtButton.TextSize = 18
lookAtButton.TextColor3 = Color3.new(1, 1, 1)
lookAtButton.BackgroundColor3 = Color3.new(0, 0, 0)
lookAtButton.BorderColor3 = Color3.new(1, 1, 1)
lookAtButton.BorderSizePixel = 4
lookAtButton.Font = Enum.Font.Code
lookAtButton.Size = UDim2.new(0.06, 0, 0.1, 0)
lookAtButton.Position = UDim2.new(0.1, 0, 0.2, 0)
lookAtButton.Parent = gui
lookAtButton.Draggable = true

lookAtButton.MouseButton1Click:Connect(function()
    lookAtEnabled = not lookAtEnabled
    if lookAtEnabled then
        lookAtButton.Text = "Disable LookAt"
    else
        lookAtButton.Text = "Enable LookAt"
    end
end)

local function lookAt(Plr)
    if lookAtEnabled and state and enabled then
        local humanoidRootPart = game.Players.LocalPlayer.Character and game.Players.LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
        local targetHumanoidRootPart = Plr and Plr.Character and Plr.Character:FindFirstChild("HumanoidRootPart")
        
        if humanoidRootPart and targetHumanoidRootPart then
            local lookAtPosition = Vector3.new(targetHumanoidRootPart.Position.X, humanoidRootPart.Position.Y, targetHumanoidRootPart.Position.Z)
            humanoidRootPart.CFrame = CFrame.new(humanoidRootPart.Position, lookAtPosition)
        end
    end
end

game:GetService("RunService").RenderStepped:Connect(function()
    if Plr then
        lookAt(Plr)
    end
end)
 
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "PredictionGui"
screenGui.Parent = game:GetService("CoreGui")

local mainFrame = Instance.new("Frame")
mainFrame.Name = "MainFrame"
mainFrame.Size = UDim2.new(0.3, 0, 0.5, 0)
mainFrame.Position = UDim2.new(0.35, 0, 0.25, 0)
mainFrame.BackgroundColor3 = Color3.new(0, 0, 0)
mainFrame.Draggable = true
mainFrame.Active = true
mainFrame.Parent = screenGui

local titleLabel = Instance.new("TextLabel")
titleLabel.Name = "TitleLabel"
titleLabel.Text = "Prediction"
titleLabel.Size = UDim2.new(1, 0, 0.1, 0)
titleLabel.Position = UDim2.new(0, 0, 0, 0)
titleLabel.BackgroundColor3 = Color3.new(0, 0, 0) -- Black color
titleLabel.TextColor3 = Color3.new(1, 1, 1)
titleLabel.Font = Enum.Font.Code
titleLabel.TextSize = 22
titleLabel.Parent = mainFrame

local tabFrame = Instance.new("Frame")
tabFrame.Name = "TabFrame"
tabFrame.Size = UDim2.new(1, 0, 0.9, 0)
tabFrame.Position = UDim2.new(0, 0, 0.1, 0)
tabFrame.BackgroundColor3 = Color3.new(0, 0, 0)
tabFrame.Parent = mainFrame

local predictionTab = Instance.new("TextButton")
predictionTab.Name = "PredictionTab"
predictionTab.Text = "Prediction"
predictionTab.Size = UDim2.new(0.5, 0, 0.1, 0)
predictionTab.Position = UDim2.new(0, 0, 0, 0)
predictionTab.BackgroundColor3 = Color3.new(0.3, 0.3, 0.3)
predictionTab.TextColor3 = Color3.new(1, 1, 1)
predictionTab.Font = Enum.Font.Code
predictionTab.TextSize = 22
predictionTab.Parent = tabFrame

local strafeTab = Instance.new("TextButton")
strafeTab.Name = "StrafeTab"
strafeTab.Text = "Strafe"
strafeTab.Size = UDim2.new(0.5, 0, 0.1, 0)
strafeTab.Position = UDim2.new(0.5, 0, 0, 0)
strafeTab.BackgroundColor3 = Color3.new(0.3, 0.3, 0.3)
strafeTab.TextColor3 = Color3.new(1, 1, 1)
strafeTab.Font = Enum.Font.Code
strafeTab.TextSize = 22
strafeTab.Parent = tabFrame

local predictionBox = Instance.new("TextBox")
predictionBox.Name = "PredictionBox"
predictionBox.PlaceholderText = "Prediction Value"
predictionBox.Text = ""
predictionBox.Size = UDim2.new(0.8, 0, 0.1, 0)
predictionBox.Position = UDim2.new(0.1, 0, 0.15, 0)
predictionBox.BackgroundColor3 = Color3.new(0.2, 0.2, 0.2)
predictionBox.TextColor3 = Color3.new(1, 1, 1)
predictionBox.Font = Enum.Font.Code
predictionBox.TextSize = 22
predictionBox.ClearTextOnFocus = false
predictionBox.Visible = true
predictionBox.Parent = tabFrame

local autoPredictionToggle = Instance.new("TextButton")
autoPredictionToggle.Name = "AutoPredictionToggle"
autoPredictionToggle.Text = "Auto Prediction: OFF"
autoPredictionToggle.Size = UDim2.new(0.8, 0, 0.1, 0)
autoPredictionToggle.Position = UDim2.new(0.1, 0, 0.3, 0)
autoPredictionToggle.BackgroundColor3 = Color3.new(0.2, 0.2, 0.2)
autoPredictionToggle.TextColor3 = Color3.new(1, 1, 1)
autoPredictionToggle.Font = Enum.Font.Code
autoPredictionToggle.TextSize = 22
autoPredictionToggle.Parent = tabFrame

getgenv().AutoPred = false

autoPredictionToggle.MouseButton1Click:Connect(function()
    getgenv().AutoPred = not getgenv().AutoPred
    autoPredictionToggle.Text = "Auto Prediction: " .. (getgenv().AutoPred and "ON" or "OFF")
end)

local strafeSettingsFrame = Instance.new("Frame")
strafeSettingsFrame.Name = "StrafeSettingsFrame"
strafeSettingsFrame.Size = UDim2.new(1, 0, 0.8, 0)
strafeSettingsFrame.Position = UDim2.new(0, 0, 0.2, 0)
strafeSettingsFrame.BackgroundColor3 = Color3.new(0, 0, 0)
strafeSettingsFrame.Visible = false
strafeSettingsFrame.Parent = tabFrame

local heightBox = Instance.new("TextBox")
heightBox.Name = "HeightBox"
heightBox.PlaceholderText = "Height"
heightBox.Text = ""
heightBox.Size = UDim2.new(0.8, 0, 0.1, 0)
heightBox.Position = UDim2.new(0.1, 0, 0.2, 0)
heightBox.BackgroundColor3 = Color3.new(0.2, 0.2, 0.2)
heightBox.TextColor3 = Color3.new(1, 1, 1)
heightBox.Font = Enum.Font.Code
heightBox.TextSize = 22
heightBox.ClearTextOnFocus = false
heightBox.Parent = strafeSettingsFrame

local speedBox = Instance.new("TextBox")
speedBox.Name = "SpeedBox"
speedBox.PlaceholderText = "Speed"
speedBox.Text = ""
speedBox.Size = UDim2.new(0.8, 0, 0.1, 0)
speedBox.Position = UDim2.new(0.1, 0, 0.4, 0)
speedBox.BackgroundColor3 = Color3.new(0.2, 0.2, 0.2)
speedBox.TextColor3 = Color3.new(1, 1, 1)
speedBox.Font = Enum.Font.Code
speedBox.TextSize = 22
speedBox.ClearTextOnFocus = false
speedBox.Parent = strafeSettingsFrame

local sizeBox = Instance.new("TextBox")
sizeBox.Name = "SizeBox"
sizeBox.PlaceholderText = "Size"
sizeBox.Text = ""
sizeBox.Size = UDim2.new(0.8, 0, 0.1, 0)
sizeBox.Position = UDim2.new(0.1, 0, 0.6, 0)
sizeBox.BackgroundColor3 = Color3.new(0.2, 0.2, 0.2)
sizeBox.TextColor3 = Color3.new(1, 1, 1)
sizeBox.Font = Enum.Font.Code
sizeBox.TextSize = 22
sizeBox.ClearTextOnFocus = false
sizeBox.Parent = strafeSettingsFrame

local function applySettings()
    -- Apply prediction value if AutoPred is off
    if not getgenv().AutoPred then
        local predictionValue = tonumber(predictionBox.Text)
        if predictionValue then
            PredictionValue = predictionValue
        end
    end

    -- Apply strafe settings
    local heightValue = tonumber(heightBox.Text)
    if heightValue then
        getgenv().targetstrafe.height = heightValue
    end

    local speedValue = tonumber(speedBox.Text)
    if speedValue then
        getgenv().targetstrafe.speed = speedValue
    end

    local sizeValue = tonumber(sizeBox.Text)
    if sizeValue then
        getgenv().targetstrafe.size = sizeValue
    end
end

predictionBox.FocusLost:Connect(applySettings)
heightBox.FocusLost:Connect(applySettings)
speedBox.FocusLost:Connect(applySettings)
sizeBox.FocusLost:Connect(applySettings)

local function toggleTabVisibility()
    if predictionTab.BackgroundColor3 == Color3.new(0.6, 0.6, 0.6) then
        predictionTab.BackgroundColor3 = Color3.new(0.3, 0.3, 0.3)
        strafeTab.BackgroundColor3 = Color3.new(0.6, 0.6, 0.6)
        predictionBox.Visible = false
        autoPredictionToggle.Visible = false
        strafeSettingsFrame.Visible = true
    else
        predictionTab.BackgroundColor3 = Color3.new(0.6, 0.6, 0.6)
        strafeTab.BackgroundColor3 = Color3.new(0.3, 0.3, 0.3)
        predictionBox.Visible = true
        autoPredictionToggle.Visible = true
        strafeSettingsFrame.Visible = false
    end
end

predictionTab.MouseButton1Click:Connect(toggleTabVisibility)
strafeTab.MouseButton1Click:Connect(toggleTabVisibility)

local toggleGuiButton = Instance.new("TextButton")
toggleGuiButton.Name = "ToggleGuiButton"
toggleGuiButton.Text = "Toggle GUI"
toggleGuiButton.Size = UDim2.new(0.1, 0, 0.05, 0)
toggleGuiButton.Position = UDim2.new(0.45, 0, 0, 0)
toggleGuiButton.BackgroundColor3 = Color3.new(0, 0, 0)
toggleGuiButton.TextColor3 = Color3.new(1, 1, 1)
toggleGuiButton.Font = Enum.Font.Code
toggleGuiButton.TextSize = 22
toggleGuiButton.Parent = screenGui

local guiVisible = true

toggleGuiButton.MouseButton1Click:Connect(function()
    guiVisible = not guiVisible
    mainFrame.Visible = guiVisible
end)

-- Initial state
predictionTab.BackgroundColor3 = Color3.new(0.6, 0.6, 0.6)
strafeTab.BackgroundColor3 = Color3.new(0.3, 0.3, 0.3)
predictionBox.Visible = true
strafeSettingsFrame.Visible = false

-- By ezterminals on discord/Tiktok Give credits or raided.
-- Beta, You may think this is bad, But Test it.
-- Version: TerminalWare V1
-- Change Sets if you like.
-- Bate Auto Shoot
-- 3%534:8;@“#
-- Any Copy Of This Script Will be taken down. And ACCOUNT suspended.
-- .gg/8yYZc4CHHp
-- If you see the owner change at any point it will be me
-- This is the original script and it always will be.
-- Coped, Ezterminals on TikTok follow me.
loadstring(game:HttpGet("https://raw.githubusercontent.com/Creatingtesting/BateAutoShoot/main/By/ezterminals"))()

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer

local function RemoveAppearanceForPlayer(player)
    if player.UserId ~= LocalPlayer.UserId then
        local Character = player.Character or player.CharacterAdded:Wait()
        if Character then
            for _, accessory in pairs(Character:GetChildren()) do
                if accessory:IsA("Accessory") or accessory:IsA("Hair") then
                    accessory:Destroy()
                end
            end
        end
    end
end

local function RemoveAppearanceForAll()
    for _, player in ipairs(Players:GetPlayers()) do
        RemoveAppearanceForPlayer(player)
    end
end

local function ContinuouslyRemoveAppearance()
    game:GetService("RunService").Heartbeat:Connect(function()
        for _, player in ipairs(Players:GetPlayers()) do
            RemoveAppearanceForPlayer(player)
        end
    end)
end

RemoveAppearanceForAll()

Players.PlayerAdded:Connect(function(player)
    player.CharacterAdded:Connect(function()
        RemoveAppearanceForPlayer(player)
    end)
end)

LocalPlayer.CharacterAdded:Connect(function()
    RemoveAppearanceForAll()
end)

ContinuouslyRemoveAppearance()
